<!DOCTYPE html>
<html>
  <head>
    <title>Tweetable</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container-sm">
      <header>
      <%= link_to image_tag("Tweetable.png"),  tweets_path, class: "title-image" %>
      <%# <%= image_tag("Icon(2).svg") %>
      <span>
      
      <% if current_user %>
        Thanks for using Tweetable, <%= current_user.name %>!
        <%= link_to "Profile", current_user, class: "button button--primary" %>

        <%= link_to "Sign Out",  destroy_user_session_path, data:{turbo_method: :delete}, class: "button button--primary" %>
      <% else %>
        <%= link_to "Sign in",   user_session_path, class: "button button--primary"   %>
        <%= link_to "Sign up",    new_user_registration_path , class: "button button--primary"   %>
      <% end %>
      </span>
      </header>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>

      <footer class="footer-container footer__content">
      <p>
      © 2021 - Codeable Tweetable <br> Build with ♥ by <strong>Angelo Coronado</strong>
      </p>
      
      </footer>
    </div>

  </body>
</html>
